server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/shop_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:admin123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect

app:
  jwt:
    secret: ${JWT_SECRET:0tpW3AuazUoAL61xcfp6jz9yUmMCKHBA04OY8Z1AwZegfu5D9q/X/C7x9JTV0GcT}
    expiration: ${JWT_EXPIRATION:900000}          # 15 phút (Access Token)
    refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000} # 7 ngày (Refresh Token)
  google:
    client-id: ${GOOGLE_CLIENT_ID:437402316397-vpo3brm7dqpa3lgkpr3fpm4hil6ga3bm.apps.googleusercontent.com}

spring.task.scheduling.pool.size: 2    # Cho phép @Scheduled chạy

cloudinary:
  cloud-name: "dskwek4ju"
  api-key: "575491814491519"
  api-secret: "t-az2VOpw3jOm1hdcD2SEF0C2gw"
